import {
    Button,
    Radio
} from "antd";

import {getAllUsers} from "./client";

import './App.css';

function App() {
    // getAllUsers()
    //     .then(res => res.json())
    //     .then(console.log);

    return (
        "Hello react"
    );
}

export default App;
